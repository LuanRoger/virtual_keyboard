name: virtual_keyboard
description: Simulate keystrokes with Win32 API.
platforms:
  windows:
version: 1.1.0
publish_to: none

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  win32: ^5.0.7
  ffi: ^2.1.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
